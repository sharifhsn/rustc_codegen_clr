[package]
name = "soak_ahash"
version = "0.1.0"
edition = "2021"
[dependencies]
ahash = "0.8"
# ahash -> getrandom 0.3 (rejects os="dotnet" without a custom backend).
getrandom = "0.3"
getrandom_dotnet = { path = "../getrandom_dotnet" }
[workspace]
