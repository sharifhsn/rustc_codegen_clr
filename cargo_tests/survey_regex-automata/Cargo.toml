[package]
name = "survey_regex-automata"
version = "0.1.0"
edition = "2021"
[dependencies]
regex-automata = { version = "0.4", default-features = false, features = ["std", "dfa", "meta", "syntax", "perf", "unicode", "nfa", "hybrid"] }
[workspace]
