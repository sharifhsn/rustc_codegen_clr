# A deterministic kitchen-sink program (local crate). Pure differential check:
# build + run native and through the backend, diff stdout+exit code.
NAME="kitchen_sink"
SRC="local"          # crate lives in ./crate
PROFILE="diff"       # native output is ground truth
# Deterministic output -> no NORMALIZE needed. No args / stdin.
